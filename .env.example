# No environment variables needed for local development!
# The SQLite database is created automatically in ./data/linkforge.db

# For production (Vercel), these are auto-populated:
# VERCEL_URL=your-app.vercel.app
